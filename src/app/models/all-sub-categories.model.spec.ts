import { AllSubCategories } from './all-sub-categories.model';

describe('AllSubCategories', () => {
  it('should create an instance', () => {
    expect(new AllSubCategories()).toBeTruthy();
  });
});
